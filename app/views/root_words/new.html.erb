<% content_for :title, "New root word" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New root word</h1>

  <%= render "form", root_word: @root_word, chapters: @chapters %>

  <%= render "shared/link_to", text: "Back to root words", path: root_words_path %>
</div>
