<% content_for :title, "Editing chapter" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing chapter</h1>

  <%= render "form", chapter: @chapter %>

  <%= render 'shared/link_to', text: "Show this chapter", path: @chapter, variant: :secondary_button %>
  <%= render 'shared/link_to', text: "Back to chapters", path: chapters_path, variant: :secondary_button %>
</div>
