<% content_for :title, "New example" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New example</h1>

  <%= render "form",
    example: @example,
    root_word: @root_word,
    root_words: @root_words,
    chapters: @chapters %>

  <%= render "shared/link_to", text: "Back to examples", path: examples_path, variant: :secondary_button %>
</div>
